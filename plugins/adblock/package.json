{
  "name": "deezmod-adblock",
  "version": "1.0.0",
  "description": "AdBlock plugin for DeezMod using pattern matching",
  "main": "adblock.js",
  "scripts": {
    "build": "node build.mjs --minify",
    "build:dev": "node build.mjs --sourcemap",
    "build:watch": "echo Watch mode not supported with custom build script. Use build:dev instead.",
    "clean": "del bundled_adblock.js bundled_adblock.js.map 2>nul || echo Clean complete",
    "prebuild": "npm run clean",
    "postbuild": "echo Build complete! Output: bundled_adblock.js"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "author": "DeezMod",
  "license": "MIT"
}
